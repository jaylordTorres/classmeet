import 'package:flutter/material.dart';

class ShiftMonitor extends StatelessWidget {
  static Icon icon = Icon(Icons.add_a_photo);
  const ShiftMonitor({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('monitor'),
    );
  }
}
